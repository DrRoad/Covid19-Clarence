(ns message.client)

(define (message (require "../server.clar/content")))
  (defn write [message, content, server]
  (if (is (load "../server.clar") true)
  (println "Client connectet succesfully with main server!")
  (defn add [message, content, server, mdb]
    (mdb.add.message)))
  
  (add(mdb.add.message))     ; message must be here declared as case e.g: (add(mdb.add."zug, max mustermann,"))
  (mdb.serv.exit))
